# הקשר פרויקט — מחולל הצעות מחיר "הדרך ללחם" (קייטרינג)

עודכן: 17 ביוני 2026

## איך להשתמש בקובץ הזה (לקלוד, בשיחה חדשה)

בשיחה חדשה: חבר/שכפל את הרֵפו וכתוב "תקרא את `PROJECT-CONTEXT.md`". מאותו רגע יש הקשר מלא על הפרויקט, גם אם השיחה והזיכרון הקודמים נמחקו. בסוף כל שיחה משמעותית — עדכן את הקובץ ודחוף אותו לגיט, כך שיישאר מעודכן וגם מגובה בענן.

> סגנון תשובה למשתמש: כל מילה באנגלית בשיחה צריכה להיכתב בשורה נפרדת לבד (המשתמש קורא עברית). שמות קבצים/קוד בגרשיים תקינים בתוך השורה.

## מה זה

אפליקציית ווב בקובץ HTML יחיד, ללא שרת וללא שלב בנייה, עברית מימין-לשמאל. מפיקה הצעת מחיר לקייטרינג (מגשי אירוח) מתוך תקציב והעדפות.

## מגבלות-על (חובה לשמר)

- **חינם + "שגר ושכח":** אין עלות חודשית, מינימום מעורבות שוטפת. כל שדרוג חייב להיות **צד-לקוח בלבד** בקובץ ה-HTML (אין שרת, אין מפתחות סודיים בקוד).
- **UX בטוח-ללקוח:** הלינק עשוי להגיע ללקוח הקצה שבונה את ההצעה בעצמו ושולח חזרה את הקובץ. לכן כל שינוי בממשק חייב להיות נקי ממידע פנימי/רגיש.

## פריסה

- רֵפו: `github.com/Neriabenarush/road-to-bread` (ציבורי — מתפרסמים רק הקבצים שבתיקיית `site/`).
- תיקיית `site/` היא מאגר הגיט. לעדכון: עורכים קובץ בתוך `site/`, ואז `git -C site add -A && git -C site commit -m "…" && git -C site push`. GitHub Pages מתעדכן תוך ~1–3 דקות.
- חי: השורש = `index.html` ; הנתיב `/premium.html` = הגרסה היוקרתית.

## שתי גרסאות

- **`premium.html` = גרסת הייצור** — מה שבעל העסק משתמש בו. שינויים מאושרים הולכים לכאן. עיצוב יוקרתי (קרם/חום, גופן Frank Ruhl Libre).
- **`index.html` = ארגז ניסיונות** — עיצוב פשוט. בודקים פה רעיונות; אם טובים → מעבירים ל-`premium`; אם לא → חוזרים אחורה.

## מה כבר נעשה ב-premium.html

- זיהוי כתובת חי בעת הקלדה (טעינה → "✓ זוהתה · כ-X ק״מ · דמי משלוח ₪Y").
- ניסוח התאמה: "מספיק ל-X אורחים בנוחות" (גם בתוצאה החיה וגם בתחתית ההצעה המודפסת).
- **סניף יחיד:** ראשון לציון — הרצל 158 (בורר הסניפים הוסר).
- **כפתור וואטסאפ הוסר** — השיתוף נעשה דרך "הדפס / שמור כ-PDF" שמייצר קובץ ממותג.

## ניסויים ב-index.html (ממתינים להחלטה אם להעביר ל-premium)

- **שכלול דמי משלוח:** מקדם כביש מדורג (קרוב <5 ק״מ → 1.8, בינוני <15 → 1.5, רחוק → 1.3); שדה "שעת אספקה"; תוספת שעת-עומס +30% **רק** כשהמסלול רחוק (מעל 12 ק״מ) וגם בחלון עומס (א׳–ה׳ 07:00–09:30 ו-16:00–19:00; ו׳ 10:00–14:00).
- עדיין עם 5 סניפים + כפתור וואטסאפ (לא סונכרן לניקוי שנעשה ב-premium).

## שיטת חישוב המרחק והדיוק

- זיהוי כתובת מול שירות חינמי (Nominatim / OpenStreetMap; ~בקשה אחת לשנייה, לא מיועד לשימוש מסחרי כבד) → מרחק אווירי (נוסחת Haversine) × מקדם כביש → תמחור מדורג לפי ק״מ. מאגר גיבוי של ~67 ערים אם הזיהוי נכשל.
- מרחק אווירי ≈ גאומטריה מדויקת, אבל נסיעה אמיתית ארוכה יותר; מקדם הכביש מקרב לנסיעה. דוגמה מאומתת: הרצל 158 → אנילביץ, אווירי ~2.3 ק״מ, נסיעה בוויז ~4.2 ק״מ → כויל מקדם קרוב ל-1.8 כך שהאומדן ≈ נסיעה.
- דיוק מושלם לכל מסלול + עומס בזמן אמת דורש שירות ניווט בתשלום (שיחשוף מפתח בקוד הציבורי, או יצריך שרת) → שובר את עקרון שגר-ושכח, ולכן לא נעשה. שדה דמי המשלוח **ניתן לעריכה ידנית** כרשת ביטחון.
- הערכת מספיקוּת: מנות לכל פריט (מגשים 10, סלטים 6, פסטות 9, קינוחים 10) מול מספר המוזמנים, עם אזהרה אם לא מספיק.

## פתוחים

- להעביר את ניסויי דמי-המשלוח ל-premium אם יאושרו.
- לוודא טיפול במע״מ לפני הוספת שורת מע״מ להצעה.
- אולי להפוך את `premium.html` לקישור השורש.
- אולי לקבע את קואורדינטת הסניף המדויקת (הקואורדינטה הקשיחה בקוד רחוקה ~0.77 ק״מ מ"הרצל 158" לפי Nominatim).
